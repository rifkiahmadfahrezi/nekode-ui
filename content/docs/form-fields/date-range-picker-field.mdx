---
title: DateRangePickerField
description: A labeled date range picker built on Calendar and Popover with a label, description, and error handling in one piece.
---

`DateRangePickerField` is the range variant of `DatePickerField` — same `Field` layout and month/year navigation, but the value is a `{ from, to }` range and the calendar runs in `mode="range"`. Reach for it when a form needs a start/end date pair (trip dates, booking windows) instead of a single date.

## Installation

<InstallationTabs componentName="date-range-picker-field" />

## Usage Example

<ComponentPreview path="src/registry/demos/date-range-picker-field-demo.tsx">
  <DateRangePickerFieldDemo />
</ComponentPreview>

## Examples

### With TanStack Form and Zod

<ComponentPreview path="src/registry/demos/date-range-picker-field-form-demo.tsx">
  <DateRangePickerFieldFormDemo />
</ComponentPreview>

## Accessibility

`DateRangePickerField` connects the label, description, and error message to the trigger button the same way `DatePickerField` does:

- The label uses `htmlFor`, so clicking it opens the calendar popover.
- `aria-describedby` points to the description, but only while there's no error.
- The error message is wired through `aria-errormessage` and `aria-invalid` flips to `true` whenever `error` is set.

## API Reference

### DateRangePickerField

| Prop | Type | Default | Description |
|---|---|---|---|
| `label` | `string` | — | Label rendered above (or beside) the trigger. |
| `description` | `string` | — | Helper text shown below the field. Hidden automatically when `error` is set. |
| `error` | `string` | — | Validation message. Switches the field into an invalid state. |
| `value` | `DateRange` | — | Controlled selected range (`{ from?: Date; to?: Date }`, from `react-day-picker`). |
| `defaultValue` | `DateRange` | — | Initial range for uncontrolled use. |
| `onValueChange` | `(range: DateRange \| undefined) => void` | — | Fires when the user picks or clears a range. |
| `placeholder` | `string` | `"Pick a date range"` | Text shown in the trigger when no range is selected. |
| `dateFormat` | `string` | `"PPP"` | A `date-fns` format string used to display each side of the range. |
| `numberOfMonths` | `number` | `2` | Number of months shown side by side in the calendar. |
| `minDate` | `Date` | — | Earliest selectable date. Disables earlier days and filters month/year options. |
| `maxDate` | `Date` | — | Latest selectable date. Disables later days and filters month/year options. |
| `name` | `string` | — | Forwarded to the trigger button for native form submission. |
| `orientation` | `"vertical" \| "horizontal" \| "responsive"` | `"vertical"` | Layout direction for the label relative to the trigger. |
| `fieldClassName` | `string` | — | Class name passed to the underlying `Field` wrapper. |
| `labelClassName` | `string` | — | Class name applied to the `FieldLabel`. |
| `triggerClassName` | `string` | — | Class name applied to the popover trigger button. |
| `required` | `boolean` | `false` | Marks the field as required and adds the asterisk indicator. |
| `disabled` | `boolean` | `false` | Disables the trigger and dims the label. |
